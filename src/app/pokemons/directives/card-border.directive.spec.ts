import { CardBorderDirective } from './card-border.directive';

describe('CardBorderDirective', () => {
  it('should create an instance', () => {
    const directive = new CardBorderDirective({} as any);
    expect(directive).toBeTruthy();
  });
});
